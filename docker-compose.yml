# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: milosz
      POSTGRES_USERNAME: postgres


  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

  jhapp:
    build: .
    restart: always
    ports:
      - 80:8080


